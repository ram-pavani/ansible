- name: variables
  hosts:  local
  connection: local
  vars:
    COURSE: "devops with aws"
    DURATION: 120hrs
    TRAINER:  ram
  tasks:
  - name: print the variables
    ansible.builtin.debug:
      msg:  "course is {{ COURSE  }}, duration {{ DURATION  }}, trainer {{ RAM }}"